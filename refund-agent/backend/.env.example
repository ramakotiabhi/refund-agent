# Copy this file to .env and fill in your real key.
# .env is gitignored -- never commit real keys to source control,
# and never paste them into a chat window, issue tracker, or Slack message.
# If a key is ever exposed that way, treat it as compromised and rotate
# it immediately in the Anthropic Console.

# Copy this file to .env and fill in your real key.
# .env is gitignored -- never commit real keys to source control,
# and never paste them into a chat window, issue tracker, or Slack message.
# If a key is ever exposed that way, treat it as compromised and rotate
# it immediately in the provider's dashboard.



# Option B: use GPT-4o instead (only used if ANTHROPIC_API_KEY is unset)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: if neither is set, the backend automatically falls back to a
# deterministic mock LLM (see backend/llm_client.py) that exercises the
# full tool-calling pipeline without any API cost. This is the default
# state of this repo as submitted.
